<section id="userguide-security">
<title>Security</title>

<para>By default, inca components use ssl to communicate with each other.  Credentials are automatically generated with the "bin/inca createauth" command described in step <xref linkend="qs-step4"> of the quickstart guide.  The createauth command creates keys and certificates for each inca component (the agent, depot, consumer and incat) and stores them in the $INCA_DIST/etc directory.</para>
<para>Inca can also be run without ssl communication (credentials then do not need to be created with the "createauth" command).  To turn off ssl communication, edit the $INCA_DIST/etc/common/inca.properties file as follows:
<orderedList>
<listItem><para>
replace all instances of "incas" with "inca"
</para></listItem>
<listItem><para>
change these lines to turn authentication off:
<screen> 16 ## authentication options:
 17 # To turn authentication off, use the below option
 18 inca.agent.auth=false
 19 
 20 # To turn authentication on, use the below options.  File paths are relative
 21 # to the classpath.  E.g., if files is at /some/file/etc/servercert.pem,
 22 # CLASSPATH must include /some/file/etc
 23 #inca.agent.auth=true
 24 #inca.agent.password=stdin:password>
 25 #inca.agent.cert=agentcert.pem
 26 #inca.agent.key=agentkey.pem
 27 #inca.agent.trusted=trusted
...
 48 ## authentication options:
 49 # To turn authentication off, use the below option
 50 inca.depot.auth=false
 51 
 52 # To turn authentication on, use the below options.  File paths are relative
 53 # to the classpath.  E.g., if files is at /some/file/etc/servercert.pem,
 54 # CLASSPATH must include /some/file/etc
 55 #inca.depot.auth=true
 56 #inca.depot.password=stdin:password>
 57 #inca.depot.cert=depotcert.pem
 58 #inca.depot.key=depotkey.pem
 59 #inca.depot.trusted=trusted
...
 85 ## authentication options:
 86 # To turn authentication off, use the below option
 87 inca.consumer.auth=false
 88 
 89 # To turn authentication on, use the below options.  File paths are relative
 90 # to the classpath.  E.g., if files is at /some/file/etc/servercert.pem,
 91 # CLASSPATH must include /some/file/etc
 92 #inca.consumer.auth=true
 93 #inca.consumer.password=stdin:password>
 94 #inca.consumer.cert=consumercert.pem
 95 #inca.consumer.key=consumerkey.pem
 96 #inca.consumer.trusted=trusted
</screen>
</para></listItem>
</orderedList>
</para>
</section>
