##
## Configuration file for TeraGrid Grids WG documentation
##

# the top level directory
TOP_DIR = @TOP_DIR@

# path to dbtools
DB2HTML = @DB2HTML@ @STYLE_OPTION@
DB2PDF = @DB2PDF@ @STYLE_OPTION@
DB2RTF = @DB2RTF@ @STYLE_OPTION@
PDF_MODE = @PDF_MODE@
HTML_MODE = @HTML_MODE@

ifdef SGML_CATALOG_FILES
	CATALOG = -c $(SGML_CATALOG_FILES)
else
	CATALOG = 
endif
	
#OUTPUT = @OUTPUT@

# documents to be created
TARGETS = @TARGETS@
